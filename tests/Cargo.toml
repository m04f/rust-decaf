[package]
name = "dcfrs-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
dcfrs = { version = "0.1.0", path = ".." }

[build-dependencies]
cc = "1.0.79"
dcfrs = { version = "0.1.0", path = ".." }
seq-macro = "0.3.3"

[dev-dependencies]
proptest = "1.1.0"
